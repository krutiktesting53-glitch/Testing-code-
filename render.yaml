services:
  - type: web
    name: krutik-ticket-support-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot1.py
    autoDeploy: true
    
